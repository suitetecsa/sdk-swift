import Foundation
struct ConnectApp {
  struct Server {
    static let baseUrl = "https://\(connectDomain):8443"
  }
}

struct NautaApp {
  struct Server {
    static let baseURL = "https://www.nauta.cu:5002"
  }
}
